import pygame
import random

pygame.init()

BLACK = (0, 0, 0)
WHITE = (255,255,255)
BLUE = (0,0,255)

size = [800, 800] #Should always be a square and multiple of 4
screen = pygame.display.set_mode(size)